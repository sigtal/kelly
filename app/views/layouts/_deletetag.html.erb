<div class="tagDeleteMenu">
  <span class="fas fa-times" id="tagDeleteClose"></span>
  <% @tags.each do |tag| %>
    <%= link_to "#{tag.category}","/tag/#{tag.id}", :method => :delete,  data: { confirm: 'Are you sure?' },class: "tagDeleteLink" %>
  <% end %>
</div>
